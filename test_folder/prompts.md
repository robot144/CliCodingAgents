
Write a python script that reads the data from                                            
  @wave_height__swan_dcsm_harmonie__Europlatform.noos into a Pandas object with columns time  
  and value, and collect the metadata from the header in a dictionary. Put the code in      
  noos_reader_mistral.py

Add a subroutine that summarizes the metadata in a human-readable format, and prints it to the console, and add the first few and last few rows of the data to the console as well.

Add a subroutine that plots the data using matplotlib, with time on the x-axis and value on the y-axis, and saves the plot as a PNG file.